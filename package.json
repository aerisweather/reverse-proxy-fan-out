{
  "name": "reverse-proxy-fan-out",
  "version": "0.0.0",
  "description": "Reverse-proxy requests to multiple servers, so that every request goes to every server",
  "main": "main.js",
  "scripts": {
    "test": "mocha --recursive test/spec"
  },
  "author": "Edan Schwartz <eschwartz@aerisweather.com>",
  "license": "UNLICENSED",
  "engines" : {
    "node" : ">=6.0.0"
  },
  "dependencies": {
    "dotenv": "^2.0.0",
    "request": "^2.75.0"
  },
  "devDependencies": {
    "co": "^4.6.0",
    "mocha": "^3.1.2",
    "supertest": "^2.0.0"
  }
}
